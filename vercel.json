{
  "buildCommand": "cd frontend && npm run build",
  "installCommand": "cd frontend && rm -f package-lock.json && npm install --legacy-peer-deps",
  "framework": "nextjs",
  "outputDirectory": "frontend/.next",
  "functions": {
    "frontend/app/**/*.js": {
      "runtime": "nodejs18.x"
    }
  }
}